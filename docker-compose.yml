version: "3.9"

services:
  smart-pricing-api:
    build:
      context: .
      args:
        BUILDKIT_INLINE_CACHE: "0"
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: unless-stopped
    